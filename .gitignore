# Datos privados (email, username, password) — NO subir a GitHub
config/private.dtsi

# Build artifacts
build/
*.uf2

# ZMK workspace (only needed for native builds, not Docker)
modules/
tools/
zephyr/
bootloader/
zmk/
.west/
